# EPUB Reader and Highlighter

[English](README.md) | [简体中文](README.zh-CN.md) | 繁體中文 | [Italiano](README.it.md)

在 Obsidian 裡直接閱讀 EPUB 電子書,用多色高亮和筆記做標記,並把所有內容匯出為 Markdown。

## 功能

- **內建 EPUB 閱讀器** — 開啟儲存庫中任意 `.epub` 檔案,不離開 Obsidian 就能閱讀。
- **捲動 / 分頁模式** — 在連續捲動和逐頁閱讀之間切換;用畫面上的按鈕或 ← / → 方向鍵翻頁。
- **固定頁碼(Apple Books 方式)** — 背景按目前視窗大小和字型預排全書,閱讀過程中總頁數固定不變,每翻一頁頁碼只 +1。首次開啟會短暫顯示「計算中」;結果會快取,只在視窗或字型變化時重算。在頁碼框輸入數字可直接跳到那一頁;捲動模式下則顯示閱讀進度百分比。
- **記住閱讀位置** — 關閉書籍(或退出 Obsidian)後重新開啟,自動回到上次讀到的段落。
- **多色高亮** — 選取文字後在彈出色板中選色,或按 **`Cmd/Ctrl+Shift+H`** 用預設顏色高亮;預設顏色跨工作階段記憶。
- **高亮筆記** — 給任意高亮附加批註。
- **相鄰高亮自動合併** — 緊接著已有高亮往後劃，兩條合併為一條；中間隔了空白和標點以外的內容則仍是兩條。
- **高亮選單** — 點擊一條高亮，就地換顏色(預設色、這本書用過的自訂色,或取色輪)、寫備註、複製或刪除。
- **批註存放在你的庫裡** — 每本書的高亮是你指定資料夾裡的一個檔案,任何同步工具都會帶上,改動一本書只傳輸那一個檔案。
- **高亮面板** — 檢視目前書籍的全部高亮,含頁碼、原文和筆記;可跳回原文位置、編輯筆記或刪除。
- **複製為引用** — 把目前選取文字或全部高亮一鍵複製成 `*頁碼 · 時間*` + 引用區塊,直接貼進筆記。
- **復原** — `Cmd/Ctrl+Z` 復原最近一次高亮或刪除。
- **閱讀主題** — 五種內建背景色,外加內嵌取色面板(無系統彈窗)自訂任意顏色;字型、字級可調;連結顏色隨背景自動適配確保可讀性。
- **全書搜尋** — 在整本書中搜尋關鍵詞,點擊結果跳轉。
- **跨頁高亮** — 標記起點後可以任意翻頁,再從後面的選取結束,一次高亮整段(限同一章內)。
- **最近使用的顏色** — 用過的自訂色隨手可取;當沒有高亮再用它時自動從清單消失。
- **匯出全書正文** — 把整本書匯出為一個檔案，或按章節分別匯出並產生索引雙鏈。已劃線的段落可選擇用 `==` 標出。
- **Markdown 匯出** — 匯出全部或選取的高亮,可指定資料夾、分組方式、排序方式以及每條高亮要帶哪些資訊。每次匯出都是一個帶日期時間的新檔案,永不覆蓋舊檔。
- **合併匯出的筆記** — 把同一本書的多份匯出合成一份:重複內容自動去除,你自己寫的內容原樣保留,原檔案不會被修改。
- **高亮快捷鍵可自訂** — 預設 `Cmd/Ctrl+Shift+H`，可在外掛設定裡改成任意組合。
- **自訂匯出模板** — 用 `{{text}}`、`{{note}}`、`{{page}}`、`{{chapter}}` 等變數決定每條高亮怎麼寫,並支援「有備註才輸出」的條件區塊。
- **首次使用引導** — 首次開啟彈出一次性的工具列圖示說明;同樣的內容也保留在外掛設定頁裡。

## 批註存放在哪裡

每本書的高亮是庫裡的一個 `.json` 檔案,位置由外掛設定中的**批註儲存**決定(預設 `epub-highlights`)。因為它們是普通的庫內檔案,你原本用的同步方式——Obsidian Sync、remotely-save、git——都會帶上它們,改動一本書也只傳輸那一本的檔案。換資料夾時既有檔案會一起搬過去。

Obsidian 的檔案列表不顯示 `.json`,所以這個資料夾在側邊欄看起來是空的,檔案確實在。用 Finder 開啟庫資料夾,或打開**選項 → 檔案與連結 → 偵測所有副檔名**。

每條高亮同時用 CFI 和它在章節文字中的位置定位。如果周圍的標記結構發生變化——換了台機器、相鄰的高亮被刪除——文字定位會重新找到這段話並修復 CFI。如果某個路徑下的 epub 被換成了另一個檔案,它的批註會被保留在一個標記為舊版的檔案裡,而不是畫在對不上的新書上。

## 安裝

### 從社群商店安裝
在 **設定 → 第三方外掛 → 瀏覽** 中搜尋 "EPUB Reader and Highlighter",安裝並啟用。

### 手動安裝
1. 從最新 [release](https://github.com/okkio-31mon/epub-reader-highlighter/releases) 下載 `main.js`、`manifest.json` 和 `styles.css`。
2. 複製到 `<你的儲存庫>/.obsidian/plugins/epub-reader-highlighter/`。
3. 重新載入 Obsidian,並在 **設定 → 第三方外掛** 中啟用。

## 使用

1. 把 `.epub` 檔案放到儲存庫中任意位置,點擊即可開啟。
2. 選取文字進行高亮 — 在彈出色板中點選顏色,或按 `Cmd/Ctrl+Shift+H` 使用預設顏色。
3. 點擊已有高亮可新增或編輯筆記。
4. 透過工具列瀏覽目錄、複製引用、開啟高亮選單、調整字型和背景、翻頁、搜尋全書。

### 工具列圖示

| 圖示 | 作用 |
| --- | --- |
| ☰(選單) | **目錄** — 檢視章節目錄,點擊跳轉。 |
| 〝(引用) | **複製引用** — 把選取文字複製成 `*頁碼 · 時間*` + 引用區塊。 |
| ▮(螢光筆) | **高亮選單** — 開啟高亮清單、複製全部、匯出(全部或所選)、設定預設高光顏色。 |
| ≡(滑桿) | **閱讀設定** — 字型、字級、背景色;點色輪展開取色面板自訂任意顏色。 |
| 捲動 / 分頁 | 在連續捲動和逐頁閱讀之間切換。 |
| ‹ 頁碼 › | 上一頁 / 下一頁(← / → 也可以)。頁碼框顯示固定頁碼 / 總頁數 · 百分比 — 輸入數字按 Enter 可跳頁;捲動模式下顯示進度百分比。 |
| 🔍 | **搜尋** — 在全書中搜尋關鍵詞並跳轉到結果。 |
| ✎(鉛筆) | **跨頁高亮 · 起點** — 標記一段話的開頭,然後翻頁。 |
| ✎✓(鉛筆加勾) | **跨頁高亮 · 結束** — 把標記的起點到目前選取之間全部高亮。 |
| ⋯ | **更多** — 介面語言和使用說明。 |

點擊已有的高亮會彈出操作列：更換顏色、寫備註、複製、刪除。

快捷鍵:`Cmd/Ctrl+Shift+H` 高亮選取文字（組合可在外掛設定裡修改） · `Cmd/Ctrl+Z` 復原。

高亮以每本書一個檔案的形式存在庫裡（見[批註存放在哪裡](#批註存放在哪裡)），閱讀位置和偏好設定存在外掛自己的資料檔案中。都不會離開你的裝置。

## 匯出與合併

在高亮選單裡選 **高光內容選擇匯出…**。彈窗上排決定*匯出哪些*:「全部」「今日」「章節選擇」,以及這本書用到的每種顏色的色點(色點的選擇按書記住)。下排是*這一次*的設定:排序方式、目標資料夾,以及一個「設為預設」勾選框——勾了才寫回設定。

同一選單裡的 **匯出為 Markdown** 不經過彈窗,直接匯出全部。

外掛設定裡存的是預設值:匯出資料夾、分組方式(不分組 / 按章節 / 按顏色)、排序方式(按書中順序或按劃線時間),以及章節、頁碼、日期、備註、顏色名稱和序號的開關。

打開 **使用自訂模板** 後,這些開關會換成一個模板框,由你決定每條的排版:`{{index}}`、`{{text}}`、`{{note}}`、`{{page}}`、`{{chapter}}`、`{{book}}`、`{{date}}`、`{{time}}`、`{{color}}`。`{{#note}}` 和 `{{/note}}` 之間的內容只在有備註時才輸出,沒備註的條目不會留下空標籤。分組標題和每條之間的分隔線仍由外掛輸出——合併功能依賴它們。

同一本書產出的所有檔案——高亮摘錄、合併摘錄、正文——都歸檔在以書名命名的資料夾裡，位於你設定的匯出資料夾下：

```
匯出資料夾/
└── 書名/
    ├── 高亮摘錄 2026-08-27-1430.md
    ├── 正文 2026-08-27-1629.md
    └── 章節選擇 2026-08-27-1640/
        ├── 索引.md
        └── 第 1 章….md
```

因為每次匯出都是獨立的帶時間戳檔案,一本書分幾次讀完就會留下幾份筆記。**合併已匯出的高亮摘錄**(命令面板,或設定裡的按鈕)可以把其中任意幾份合成一份:選擇檔案、分組和排序,預覽結果,然後寫入一個新檔案。重複的高亮只保留一條;對應不上任何高亮的內容——你自己寫的,或被你改動過的原文——保留在「其他內容」裡。原檔案始終不會被修改。

## 匯出全書正文

高亮選單裡還有 **匯出全書正文…**。兩種佈局任選：

- **整本一個檔案** — 章節以 `##` 標題排在同一個檔案裡。
- **一章一個檔案** — 每章一個檔案，另加一個按閱讀順序連結各章的索引檔案。

勾選要匯出的章節；封面、版權頁、書自帶的目錄頁也會列出來，通常可以不勾。圖片會略過，書內連結只保留文字。

**標註已高亮的內容**會把你劃過線的段落用 `==` 包起來。顏色不會帶過來——顏色保留在高亮摘錄裡。匯出後會提示比對到多少條；措辭已經和轉換後的正文對不上的高亮會被跳過，不會靜默遺失。

## 開發

```bash
npm install
npm run dev    # 監看建置
npm run build  # 正式建置
```

## 致謝

EPUB 渲染基於 [epub.js](https://github.com/futurepress/epub.js)。

## 授權

[MIT](LICENSE)
